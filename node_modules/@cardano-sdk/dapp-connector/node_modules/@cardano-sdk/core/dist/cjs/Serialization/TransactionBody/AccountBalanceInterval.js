"use strict";
var __classPrivateFieldSet = (this && this.__classPrivateFieldSet) || function (receiver, state, value, kind, f) {
    if (kind === "m") throw new TypeError("Private method is not writable");
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
    return (kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value)), value;
};
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _AccountBalanceInterval_inclusiveLowerBound, _AccountBalanceInterval_exclusiveUpperBound, _AccountBalanceInterval_originalBytes;
Object.defineProperty(exports, "__esModule", { value: true });
exports.AccountBalanceInterval = void 0;
const CBOR_1 = require("../CBOR");
const util_1 = require("@cardano-sdk/util");
const errors_1 = require("../../errors");
const INTERVAL_ARRAY_SIZE = 2;
const BOTH_NIL_MESSAGE = 'Both interval bounds cannot be nil.';
class AccountBalanceInterval {
    constructor(inclusiveLowerBound, exclusiveUpperBound) {
        _AccountBalanceInterval_inclusiveLowerBound.set(this, void 0);
        _AccountBalanceInterval_exclusiveUpperBound.set(this, void 0);
        _AccountBalanceInterval_originalBytes.set(this, undefined);
        __classPrivateFieldSet(this, _AccountBalanceInterval_inclusiveLowerBound, inclusiveLowerBound, "f");
        __classPrivateFieldSet(this, _AccountBalanceInterval_exclusiveUpperBound, exclusiveUpperBound, "f");
    }
    toCbor() {
        if (__classPrivateFieldGet(this, _AccountBalanceInterval_originalBytes, "f"))
            return __classPrivateFieldGet(this, _AccountBalanceInterval_originalBytes, "f");
        if (__classPrivateFieldGet(this, _AccountBalanceInterval_inclusiveLowerBound, "f") === undefined && __classPrivateFieldGet(this, _AccountBalanceInterval_exclusiveUpperBound, "f") === undefined)
            throw new util_1.InvalidStateError(BOTH_NIL_MESSAGE);
        const writer = new CBOR_1.CborWriter();
        writer.writeStartArray(INTERVAL_ARRAY_SIZE);
        if (__classPrivateFieldGet(this, _AccountBalanceInterval_inclusiveLowerBound, "f") === undefined) {
            writer.writeNull();
        }
        else {
            writer.writeInt(__classPrivateFieldGet(this, _AccountBalanceInterval_inclusiveLowerBound, "f"));
        }
        if (__classPrivateFieldGet(this, _AccountBalanceInterval_exclusiveUpperBound, "f") === undefined) {
            writer.writeNull();
        }
        else {
            writer.writeInt(__classPrivateFieldGet(this, _AccountBalanceInterval_exclusiveUpperBound, "f"));
        }
        return writer.encodeAsHex();
    }
    static fromCbor(cbor) {
        const reader = new CBOR_1.CborReader(cbor);
        const length = reader.readStartArray();
        if (length !== INTERVAL_ARRAY_SIZE)
            throw new errors_1.SerializationError(errors_1.SerializationFailure.InvalidType, `account_balance_interval must be an array of ${INTERVAL_ARRAY_SIZE} elements, but got ${length}`);
        let inclusiveLowerBound;
        if (reader.peekState() === CBOR_1.CborReaderState.Null) {
            reader.readNull();
        }
        else {
            inclusiveLowerBound = reader.readInt();
        }
        let exclusiveUpperBound;
        if (reader.peekState() === CBOR_1.CborReaderState.Null) {
            reader.readNull();
        }
        else {
            exclusiveUpperBound = reader.readInt();
        }
        reader.readEndArray();
        if (inclusiveLowerBound === undefined && exclusiveUpperBound === undefined)
            throw new errors_1.SerializationError(errors_1.SerializationFailure.InvalidType, BOTH_NIL_MESSAGE);
        const interval = new AccountBalanceInterval(inclusiveLowerBound, exclusiveUpperBound);
        __classPrivateFieldSet(interval, _AccountBalanceInterval_originalBytes, cbor, "f");
        return interval;
    }
    toCore() {
        return {
            ...(__classPrivateFieldGet(this, _AccountBalanceInterval_inclusiveLowerBound, "f") !== undefined && { inclusiveLowerBound: __classPrivateFieldGet(this, _AccountBalanceInterval_inclusiveLowerBound, "f") }),
            ...(__classPrivateFieldGet(this, _AccountBalanceInterval_exclusiveUpperBound, "f") !== undefined && { exclusiveUpperBound: __classPrivateFieldGet(this, _AccountBalanceInterval_exclusiveUpperBound, "f") })
        };
    }
    static fromCore(interval) {
        if (interval.inclusiveLowerBound === undefined && interval.exclusiveUpperBound === undefined)
            throw new util_1.InvalidArgumentError('interval', BOTH_NIL_MESSAGE);
        return new AccountBalanceInterval(interval.inclusiveLowerBound, interval.exclusiveUpperBound);
    }
    setInclusiveLowerBound(inclusiveLowerBound) {
        __classPrivateFieldSet(this, _AccountBalanceInterval_inclusiveLowerBound, inclusiveLowerBound, "f");
        __classPrivateFieldSet(this, _AccountBalanceInterval_originalBytes, undefined, "f");
    }
    inclusiveLowerBound() {
        return __classPrivateFieldGet(this, _AccountBalanceInterval_inclusiveLowerBound, "f");
    }
    setExclusiveUpperBound(exclusiveUpperBound) {
        __classPrivateFieldSet(this, _AccountBalanceInterval_exclusiveUpperBound, exclusiveUpperBound, "f");
        __classPrivateFieldSet(this, _AccountBalanceInterval_originalBytes, undefined, "f");
    }
    exclusiveUpperBound() {
        return __classPrivateFieldGet(this, _AccountBalanceInterval_exclusiveUpperBound, "f");
    }
}
exports.AccountBalanceInterval = AccountBalanceInterval;
_AccountBalanceInterval_inclusiveLowerBound = new WeakMap(), _AccountBalanceInterval_exclusiveUpperBound = new WeakMap(), _AccountBalanceInterval_originalBytes = new WeakMap();
//# sourceMappingURL=AccountBalanceInterval.js.map