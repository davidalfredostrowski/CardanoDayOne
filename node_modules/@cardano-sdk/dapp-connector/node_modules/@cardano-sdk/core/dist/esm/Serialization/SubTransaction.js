var __classPrivateFieldSet = (this && this.__classPrivateFieldSet) || function (receiver, state, value, kind, f) {
    if (kind === "m") throw new TypeError("Private method is not writable");
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
    return (kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value)), value;
};
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _SubTransaction_body, _SubTransaction_witnessSet, _SubTransaction_auxiliaryData, _SubTransaction_originalBytes;
import * as Crypto from '@cardano-sdk/crypto';
import { AuxiliaryData } from './AuxiliaryData/index.js';
import { CborReader, CborReaderState, CborWriter } from './CBOR/index.js';
import { HexBlob } from '@cardano-sdk/util';
import { SerializationError, SerializationFailure } from '../errors.js';
import { SubTransactionBody } from './TransactionBody/SubTransactionBody.js';
import { TransactionWitnessSet } from './TransactionWitnessSet/index.js';
import { hexToBytes } from '../util/misc/index.js';
const SUB_TX_FRAME_SIZE = 3;
export class SubTransaction {
    constructor(body, witnessSet, auxiliaryData) {
        _SubTransaction_body.set(this, void 0);
        _SubTransaction_witnessSet.set(this, void 0);
        _SubTransaction_auxiliaryData.set(this, void 0);
        _SubTransaction_originalBytes.set(this, undefined);
        __classPrivateFieldSet(this, _SubTransaction_body, body, "f");
        __classPrivateFieldSet(this, _SubTransaction_witnessSet, witnessSet, "f");
        __classPrivateFieldSet(this, _SubTransaction_auxiliaryData, auxiliaryData, "f");
    }
    toCbor() {
        if (__classPrivateFieldGet(this, _SubTransaction_originalBytes, "f"))
            return __classPrivateFieldGet(this, _SubTransaction_originalBytes, "f");
        const writer = new CborWriter();
        writer.writeStartArray(SUB_TX_FRAME_SIZE);
        writer.writeEncodedValue(hexToBytes(__classPrivateFieldGet(this, _SubTransaction_body, "f").toCbor()));
        writer.writeEncodedValue(hexToBytes(__classPrivateFieldGet(this, _SubTransaction_witnessSet, "f").toCbor()));
        if (__classPrivateFieldGet(this, _SubTransaction_auxiliaryData, "f")) {
            writer.writeEncodedValue(hexToBytes(__classPrivateFieldGet(this, _SubTransaction_auxiliaryData, "f").toCbor()));
        }
        else {
            writer.writeNull();
        }
        return writer.encodeAsHex();
    }
    static fromCbor(cbor, options) {
        const reader = new CborReader(cbor);
        const length = reader.readStartArray();
        if (length !== null && length !== SUB_TX_FRAME_SIZE)
            throw new SerializationError(SerializationFailure.InvalidType, `Sub transaction frame must be exactly ${SUB_TX_FRAME_SIZE} elements (there is no is_valid flag), found ${length}`);
        const body = SubTransactionBody.fromCbor(HexBlob.fromBytes(reader.readEncodedValue()), options);
        const witnessSet = TransactionWitnessSet.fromCbor(HexBlob.fromBytes(reader.readEncodedValue()), options);
        let auxData;
        if (reader.peekState() === CborReaderState.Null) {
            reader.readNull();
        }
        else {
            auxData = AuxiliaryData.fromCbor(HexBlob.fromBytes(reader.readEncodedValue()));
        }
        reader.readEndArray();
        const subTx = new SubTransaction(body, witnessSet, auxData);
        __classPrivateFieldSet(subTx, _SubTransaction_originalBytes, cbor, "f");
        return subTx;
    }
    toCore() {
        const subTx = {
            body: __classPrivateFieldGet(this, _SubTransaction_body, "f").toCore(),
            witness: __classPrivateFieldGet(this, _SubTransaction_witnessSet, "f").toCore()
        };
        if (__classPrivateFieldGet(this, _SubTransaction_auxiliaryData, "f")) {
            subTx.auxiliaryData = __classPrivateFieldGet(this, _SubTransaction_auxiliaryData, "f").toCore();
        }
        return subTx;
    }
    static fromCore(subTx) {
        return new SubTransaction(SubTransactionBody.fromCore(subTx.body), TransactionWitnessSet.fromCore(subTx.witness), subTx.auxiliaryData ? AuxiliaryData.fromCore(subTx.auxiliaryData) : undefined);
    }
    body() {
        return SubTransactionBody.fromCbor(__classPrivateFieldGet(this, _SubTransaction_body, "f").toCbor());
    }
    setBody(body) {
        __classPrivateFieldSet(this, _SubTransaction_body, body, "f");
        __classPrivateFieldSet(this, _SubTransaction_originalBytes, undefined, "f");
    }
    witnessSet() {
        return TransactionWitnessSet.fromCbor(__classPrivateFieldGet(this, _SubTransaction_witnessSet, "f").toCbor());
    }
    setWitnessSet(witnessSet) {
        __classPrivateFieldSet(this, _SubTransaction_witnessSet, witnessSet, "f");
        __classPrivateFieldSet(this, _SubTransaction_originalBytes, undefined, "f");
    }
    auxiliaryData() {
        if (__classPrivateFieldGet(this, _SubTransaction_auxiliaryData, "f")) {
            return AuxiliaryData.fromCbor(__classPrivateFieldGet(this, _SubTransaction_auxiliaryData, "f").toCbor());
        }
        return undefined;
    }
    setAuxiliaryData(auxiliaryData) {
        __classPrivateFieldSet(this, _SubTransaction_auxiliaryData, auxiliaryData, "f");
        __classPrivateFieldSet(this, _SubTransaction_originalBytes, undefined, "f");
    }
    getId() {
        return Crypto.blake2b.hash(__classPrivateFieldGet(this, _SubTransaction_body, "f").toCbor(), 32);
    }
    clone() {
        const bytes = this.toCbor();
        return SubTransaction.fromCbor(bytes);
    }
}
_SubTransaction_body = new WeakMap(), _SubTransaction_witnessSet = new WeakMap(), _SubTransaction_auxiliaryData = new WeakMap(), _SubTransaction_originalBytes = new WeakMap();
//# sourceMappingURL=SubTransaction.js.map