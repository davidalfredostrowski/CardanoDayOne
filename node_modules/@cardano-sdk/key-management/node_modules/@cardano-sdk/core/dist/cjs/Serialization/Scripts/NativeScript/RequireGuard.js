"use strict";
var __classPrivateFieldSet = (this && this.__classPrivateFieldSet) || function (receiver, state, value, kind, f) {
    if (kind === "m") throw new TypeError("Private method is not writable");
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
    return (kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value)), value;
};
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _RequireGuard_credential, _RequireGuard_originalBytes;
Object.defineProperty(exports, "__esModule", { value: true });
exports.RequireGuard = void 0;
const CBOR_1 = require("../../CBOR");
const Credential_1 = require("../../Common/Credential");
const util_1 = require("@cardano-sdk/util");
const Script_1 = require("../../../Cardano/types/Script");
const EMBEDDED_GROUP_SIZE = 2;
class RequireGuard {
    constructor(credential) {
        _RequireGuard_credential.set(this, void 0);
        _RequireGuard_originalBytes.set(this, undefined);
        __classPrivateFieldSet(this, _RequireGuard_credential, credential, "f");
    }
    toCbor() {
        const writer = new CBOR_1.CborWriter();
        if (__classPrivateFieldGet(this, _RequireGuard_originalBytes, "f"))
            return __classPrivateFieldGet(this, _RequireGuard_originalBytes, "f");
        writer.writeStartArray(EMBEDDED_GROUP_SIZE);
        writer.writeInt(Script_1.NativeScriptKind.RequireGuard);
        writer.writeEncodedValue(Buffer.from(__classPrivateFieldGet(this, _RequireGuard_credential, "f").toCbor(), 'hex'));
        return writer.encodeAsHex();
    }
    static fromCbor(cbor) {
        const reader = new CBOR_1.CborReader(cbor);
        const length = reader.readStartArray();
        if (length !== EMBEDDED_GROUP_SIZE)
            throw new util_1.InvalidArgumentError('cbor', `Expected an array of two elements, but got an array of ${length} elements`);
        const kind = Number(reader.readInt());
        if (kind !== Script_1.NativeScriptKind.RequireGuard)
            throw new util_1.InvalidArgumentError('cbor', `Expected kind ${Script_1.NativeScriptKind.RequireGuard}, but got kind ${kind}`);
        const credential = Credential_1.Credential.fromCbor(util_1.HexBlob.fromBytes(reader.readEncodedValue()));
        const script = new RequireGuard(credential);
        __classPrivateFieldSet(script, _RequireGuard_originalBytes, cbor, "f");
        return script;
    }
    toCore() {
        return {
            __type: Script_1.ScriptType.Native,
            credential: __classPrivateFieldGet(this, _RequireGuard_credential, "f").toCore(),
            kind: Script_1.NativeScriptKind.RequireGuard
        };
    }
    static fromCore(script) {
        return new RequireGuard(Credential_1.Credential.fromCore(script.credential));
    }
    credential() {
        return __classPrivateFieldGet(this, _RequireGuard_credential, "f");
    }
    setCredential(credential) {
        __classPrivateFieldSet(this, _RequireGuard_credential, credential, "f");
        __classPrivateFieldSet(this, _RequireGuard_originalBytes, undefined, "f");
    }
}
exports.RequireGuard = RequireGuard;
_RequireGuard_credential = new WeakMap(), _RequireGuard_originalBytes = new WeakMap();
//# sourceMappingURL=RequireGuard.js.map