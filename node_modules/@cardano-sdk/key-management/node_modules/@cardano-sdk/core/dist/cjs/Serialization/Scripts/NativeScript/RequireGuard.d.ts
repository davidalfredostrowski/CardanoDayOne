import { Credential } from '../../Common/Credential';
import { HexBlob } from '@cardano-sdk/util';
import type * as Cardano from '../../../Cardano';
export declare class RequireGuard {
    #private;
    constructor(credential: Credential);
    toCbor(): HexBlob;
    static fromCbor(cbor: HexBlob): RequireGuard;
    toCore(): Cardano.RequireGuardScript;
    static fromCore(script: Cardano.RequireGuardScript): RequireGuard;
    credential(): Credential;
    setCredential(credential: Credential): void;
}
//# sourceMappingURL=RequireGuard.d.ts.map