var __classPrivateFieldSet = (this && this.__classPrivateFieldSet) || function (receiver, state, value, kind, f) {
    if (kind === "m") throw new TypeError("Private method is not writable");
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a setter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
    return (kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value)), value;
};
var __classPrivateFieldGet = (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
    if (kind === "a" && !f) throw new TypeError("Private accessor was defined without a getter");
    if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
    return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
};
var _Guards_keyHashes, _Guards_credentials;
import { CborReader, CborReaderState, CborTag } from '../CBOR/index.js';
import { CborSet, Credential, Hash } from '../Common/index.js';
import { InvalidArgumentError, InvalidStateError } from '@cardano-sdk/util';
export var GuardsKind;
(function (GuardsKind) {
    GuardsKind[GuardsKind["KeyHashes"] = 0] = "KeyHashes";
    GuardsKind[GuardsKind["Credentials"] = 1] = "Credentials";
})(GuardsKind || (GuardsKind = {}));
export class Guards {
    constructor(keyHashes, credentials) {
        _Guards_keyHashes.set(this, void 0);
        _Guards_credentials.set(this, void 0);
        __classPrivateFieldSet(this, _Guards_keyHashes, keyHashes, "f");
        __classPrivateFieldSet(this, _Guards_credentials, credentials, "f");
    }
    toCbor() {
        if (__classPrivateFieldGet(this, _Guards_keyHashes, "f"))
            return __classPrivateFieldGet(this, _Guards_keyHashes, "f").toCbor();
        if (__classPrivateFieldGet(this, _Guards_credentials, "f"))
            return __classPrivateFieldGet(this, _Guards_credentials, "f").toCbor();
        throw new InvalidStateError('Guards instance has no values');
    }
    static fromCbor(cbor) {
        const reader = new CborReader(cbor);
        if (reader.peekState() === CborReaderState.Tag && reader.peekTag() === CborTag.Set)
            reader.readTag();
        reader.readStartArray();
        if (reader.peekState() === CborReaderState.StartArray)
            return new Guards(undefined, CborSet.fromCbor(cbor, Credential.fromCbor));
        return new Guards(CborSet.fromCbor(cbor, Hash.fromCbor));
    }
    static fromKeyHashes(keyHashes) {
        return new Guards(CborSet.fromCore(keyHashes, Hash.fromCore));
    }
    static fromCredentials(credentials) {
        if (credentials.length === 0)
            throw new InvalidArgumentError('credentials', 'Credential form guards must contain at least one element');
        return new Guards(undefined, CborSet.fromCore(credentials, Credential.fromCore));
    }
    kind() {
        return __classPrivateFieldGet(this, _Guards_keyHashes, "f") ? GuardsKind.KeyHashes : GuardsKind.Credentials;
    }
    keyHashes() {
        return __classPrivateFieldGet(this, _Guards_keyHashes, "f")?.toCore();
    }
    credentials() {
        return __classPrivateFieldGet(this, _Guards_credentials, "f")?.toCore();
    }
    size() {
        return __classPrivateFieldGet(this, _Guards_keyHashes, "f") ? __classPrivateFieldGet(this, _Guards_keyHashes, "f").size() : __classPrivateFieldGet(this, _Guards_credentials, "f") ? __classPrivateFieldGet(this, _Guards_credentials, "f").size() : 0;
    }
}
_Guards_keyHashes = new WeakMap(), _Guards_credentials = new WeakMap();
//# sourceMappingURL=Guards.js.map